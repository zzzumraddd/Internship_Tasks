apiVersion: batch/v1
kind: Job
metadata:
  name: print-date
  namespace: anyops-cron
spec:
  template:
    spec:
      containers:
        - name: print-date
          image: busybox:latest
          imagePullPolicy: IfNotPresent
          command:
            - date
      restartPolicy: Never
  backoffLimit: 0
